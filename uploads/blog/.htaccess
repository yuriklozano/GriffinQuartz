# Griffin Quartz Blog Uploads - Security
# Only allow image files to be served

# Disable PHP execution
<FilesMatch "\.php$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Only allow specific image types
<FilesMatch "(?i)\.(jpg|jpeg|png|gif|webp)$">
    Order allow,deny
    Allow from all
</FilesMatch>

# Disable directory listing
Options -Indexes
